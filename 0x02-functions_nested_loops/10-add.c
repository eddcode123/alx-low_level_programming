#include <stdio.h>
#include "main.h"

/**
 * add - adds two integers and returns the result
 * @x: number to be added
 * @y: second number to be add
 * Return: sum of x and y
 */
int add(int x, int y)
{
	/* declare a variable sum to fimd sum of x and y */
	int sum;

	sum = x + y;
	return (sum);
}
