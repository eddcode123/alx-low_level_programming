#include <stdlib.h>
#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 * @a: integer to convert to its absolute value
 *
 * Return: Absolute int value
 */
int _abs(int a)
{
	int b;

	/*conver value to absolute */
	b = abs(a);
	return (b);
}
