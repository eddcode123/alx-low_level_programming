#include <stdio.h>
/**
 * main - print base 10 numbers
 * Return: Always(0)
*/

int main(void)
{
    int i;

    for (i = 0; i < 10; i++)
    {
        putchar(i);
    }
    putchar('\n');

    return (0);
}
